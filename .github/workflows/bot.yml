name: Repository[bot]
on:
  workflow_call:
    secrets:
      token:
        required: true

jobs:
  repository-bot:
    name: Repository[bot]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: >-
      ${{
      (github.repository_owner == 'Katsute' || github.repository_owner == 'KatsuteDev') &&
      github.actor != 'mashiro-san' &&
      !endsWith(github.actor, '[bot]')
      }}
    steps:
      - name: 📤 Checkout Repository
        uses: Katsute/Workflows/actions/checkout@main

      - name: 📤 Checkout Action
        uses: actions/checkout@v3
        with:
          repository: Katsute/Repository-Bot
          ref: e50074f97faf020b7af7f36cbe1188bac47c492b
          path: ./.github/repository-bot
          token: ${{ secrets.token }}

      - name: ✔️ Handle Event
        uses: ./.github/repository-bot
        with:
          token: ${{ secrets.token }}
