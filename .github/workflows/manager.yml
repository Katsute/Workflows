name: Repository Manager
on:
  workflow_call:
    secrets:
      token:
        required: true

jobs:
  manager:
    name: Repository Manager
    uses: Katsute/Repository-Manager/.github/workflows/repository-manager.yml@fb7dc5d3e37f28bd5a45642bb598c61f662da0c5
    if: >-
      ${{
      (github.repository_owner == 'Katsute' || github.repository_owner == 'KatsuteDev') &&
      github.repository != 'Katsute/Repository-Manager' &&
      github.actor != 'mashiro-san' &&
      !endsWith(github.actor, '[bot]')
      }}
    with:
      login: mashiro-san
      username: Mashiro
      email: 107276926+mashiro-san@users.noreply.github.com
    secrets:
      token: ${{ secrets.token }}