name: Sinon[bot]
on:
  workflow_call:
    secrets:
      token:
        required: true

jobs:
  sinon:
    name: Sinon[bot]
    runs-on: ubuntu-latest
    if: |
      (github.repository_owner == 'Katsute' || github.repository_owner == 'KatsuteDev') &&
      github.actor != 'Sinon-bot' &&
      github.actor != 'dependabot[bot]' &&
      github.actor != 'dependabot-preview[bot]'
    steps:
      - name: 📤 Checkout Repository
        uses: Katsute/Workflows/Actions/checkout@main

      - name: 📤 Checkout Action
        uses: actions/checkout@v3
        with:
          repository: Katsute/Sinon
          ref: 7db07850744db125b499e0dc0656c49b4f7807a3
          path: ./.github/sinon
          token: ${{ secrets.token }}

      - name: ✔ Handle Event
        uses: ./.github/sinon
        with:
          token: ${{ secrets.token }}
