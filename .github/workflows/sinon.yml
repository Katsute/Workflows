name: Sinon[bot]
on:
  workflow_call:
    secrets:
      token:
        required: true

jobs:
  sinon:
    name: Sinon[bot]
    runs-on: ubuntu-latest
    if: |
      (github.repository_owner == 'Katsute' || github.repository_owner == 'KatsuteDev') &&
      github.actor != 'Sinon-bot' &&
      github.actor != 'dependabot[bot]' &&
      github.actor != 'dependabot-preview[bot]'
    steps:
      - name: 📤 Checkout Repository
        uses: Katsute/Workflows/Actions/checkout@main

      - name: 📤 Checkout Action
        uses: actions/checkout@v3
        with:
          repository: Katsute/Sinon
          ref: 2100cc4e8ba709d3ed879ccaffa5afa46e920e1d
          path: ./.github/sinon
          token: ${{ secrets.token }}

      - name: ✔ Handle Event
        uses: ./.github/sinon
        with:
          token: ${{ secrets.token }}
