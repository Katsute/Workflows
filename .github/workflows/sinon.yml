name: Repository[bot]
on:
  workflow_call:
    secrets:
      token:
        required: true

jobs:
  repository-bot:
    name: Repository[bot]
    runs-on: ubuntu-latest
    if: |
      (github.repository_owner == 'Katsute' || github.repository_owner == 'KatsuteDev') &&
      github.actor != 'Sinon-bot' &&
      github.actor != 'dependabot[bot]' &&
      github.actor != 'dependabot-preview[bot]'
    steps:
      - name: 📤 Checkout Repository
        uses: Katsute/Workflows/Actions/checkout@main

      - name: 📤 Checkout Action
        uses: actions/checkout@v3
        with:
          repository: Katsute/Repository-Bot
          ref: 394fdc7ca4dc5573ec2f105d55ddc3f4731d7405
          path: ./.github/repository-bot
          token: ${{ secrets.token }}

      - name: ✔ Handle Event
        uses: ./.github/repository-bot
        with:
          token: ${{ secrets.token }}
